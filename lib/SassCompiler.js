
class SassCompiler {

  async render(contents, options = {}) {
    return Promise.reject(new Error('@todo install node-sass'));
  }
}

module.exports = SassCompiler;
module.exports.default = SassCompiler;
